services:
  - type: web
    name: mining-quality-api
    env: python
    buildCommand: pip install -r deployment/requirements.txt
    startCommand: uvicorn api.main:app --host 0.0.0.0 --port 10000
    plan: free